<footer class="container-fluid p-y-lg white-bg text-muted">
    <section class="row">
        <div class="col-xs-5 col-xs-offset-1 col-sm-2">
            <h6 class="m-b-md">SECTIONS</h6>

            {% for link in linklists.section.links %}
                  <a class="block" href="{{ link.url }}">{{ link.title }}</a>
            {% endfor %}

        </div>
        <div class="col-xs-5 col-sm-2">
            <h6 class="m-b-md">FOLLOW</h6>

            <a class="block" data-toggle="modal" data-target="#subscribeModal">Subscribe to our newsletter</a>
            {% for link in linklists.follow.links %}
                  <a class="block" href="{{ link.url }}" target="_blank">{{ link.title }}</a>
            {% endfor %}

        </div>
        <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4">
            <h6 class="m-b-md">WHAT WE BELIEVE</h6>
            <p class="lead">Making and consuming things is how this whole crazy beautiful parade we call life works. At Supply we believe that if it’s made good it feels good too.</p>

            {% comment %} Loop through available payment methods and show their icons. {% endcomment %}
            {% unless shop.enabled_payment_types == empty %}
            <hr>
            <h4 class="text-center">{{ 'layout.footer.accepted_payments' | t }}</h4>
            <ul class="inline-list payment-icons">
                {% for type in shop.enabled_payment_types %}
                <li>
                    <span class="icon-fallback-text">
                        <span class="icon icon-{{type}}" aria-hidden="true"></span>
                        <span class="fallback-text">{{ type | replace: '_',' '}}</span>
                    </span>
                </li>
                {% endfor %}
            </ul>
            {% endunless %}
        </div>
        <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-2 text-center">
            <h6 class="m-b-md">LEGAL</h6>
            © {{ 'now' | date: "%Y" }} Supply</br>
        </div>
    </section>
</footer>
